# Shared cursor theme values for home.pointerCursor, compositor cursor
# blocks, GTK/dconf, session variables, and the Steam FHS env.
{
  name = "BreezeX-RosePine-Linux";
  size = 32;
  packageAttr = "rose-pine-cursor";
}
